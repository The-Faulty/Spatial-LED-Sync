[build-system]
requires = ["setuptools>=68", "wheel", "pybind11>=2.12"]
build-backend = "setuptools.build_meta"

[project]
name = "ambilight-effects-native"
version = "0.1.0"
description = "Optional native renderer acceleration for Ambilight Effects"
requires-python = ">=3.10"
